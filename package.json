{
  "name": "haffaz",
  "version": "0.0.1",
  "main": "node_modules/dictator-builder/lib/index.js",
  "bin": {
    "haffaz": "node_modules/dictator-builder/lib/index.js"
  },
  "scripts": {
    "run": "npm run build && npm i -g && haffaz",
    "build": "npm run format",
    "format": "prettier --write '{*.md,package.json}'"
  },
  "dependencies": {
    "dictator-builder": "^0.0.1"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
