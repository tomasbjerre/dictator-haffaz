{
  "name": "haffaz",
  "version": "0.0.1",
  "bin": {
    "haffaz": "node_modules/dictator-builder/lib/index.js"
  },
  "scripts": {
    "run": "npm run build && npm i -g && haffaz",
    "build": "npm run format",
    "format": "npx prettier --write '{*.md,package.json,**/*.json}'",
    "release": "npm version patch && npm run test && npm publish && git push"
  },
  "dependencies": {
    "dictator-builder": "0.0.5"
  }
}
